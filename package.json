{
  "name": "my-project",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "firebase": "^6.2.4",
    "node-sass": "^4.12.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.1",
    "react-with-firebase-auth": "^1.0.3",
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "rm -rf build docs && react-scripts build && mv build docs",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "dev": "run-p --race firebase serve -p 4000 react-scripts start",
    "dev:firebase": "",
    "dev:react": "react-scripts start"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
